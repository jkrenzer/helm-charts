{{- if .Values.persistentVolume.create -}}
{{- include "common-library.persistentVolume" (dict "globals" . "override" "openldap.persistentVolume") -}}
{{- end }}
{{- define "openldap.persistentVolume" -}}
{{- end }}